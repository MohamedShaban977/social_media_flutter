import 'package:flutter/cupertino.dart';

class DiscoverPostsWidget extends StatelessWidget {
  const DiscoverPostsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
